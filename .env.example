VITE_SANITY_PROJECT_ID=your-project-id
VITE_SANITY_DATASET=production

# Only needed locally for scripts/seed-sanity.mjs — never set this in Vercel.
# Generate at sanity.io/manage -> API -> Tokens (Editor permission).
SANITY_WRITE_TOKEN=
